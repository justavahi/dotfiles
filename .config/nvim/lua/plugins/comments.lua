-- Commenting shortcuts

return {
    'numToStr/Comment.nvim',
    opts = {
      padding = true, -- ADD SPACE AFTER COMMENT
      sticky  = true, -- SAVE CURSOR POSITION AFTER COMMENTING
      ignore  = "^$"  -- SKIP EMPTY LINES
    }
}
